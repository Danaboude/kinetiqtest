<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Andriod_test" parent="android:Theme.Material.Light.NoActionBar.Fullscreen">
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">@color/kinetiq_background</item>
        <!-- Navigation bar color. -->
        <item name="android:navigationBarColor" tools:targetApi="l">@color/kinetiq_background</item>
        <!-- Window background color. -->
        <item name="android:windowBackground">@color/kinetiq_background</item>
        <!-- Text colors -->
        <item name="android:textColorPrimary">@color/kinetiq_text_primary</item>
        <item name="android:textColorSecondary">@color/kinetiq_text_primary</item>
    </style>
</resources>